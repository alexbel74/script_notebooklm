# 🦊 Firefox Installation - РЕШЕНИЕ!

## ❗ ПРОБЛЕМА НАЙДЕНА!

Firefox автоматически ищет файл `manifest.json` в папке. Если ты выбираешь всю папку проекта, Firefox находит Chrome-версию манифеста (с `service_worker`), и это вызывает ошибку!

## ✅ РЕШЕНИЕ - 2 ВАРИАНТА:

---

## 🎯 **ВАРИАНТ 1: Использовать специальную Firefox-папку** (РЕКОМЕНДУЕТСЯ!)

### Шаг 1: Скачай проект
```
https://github.com/alexbel74/script_notebooklm
Code → Download ZIP → Распакуй
```

### Шаг 2: Найди папку `firefox-build`
В распакованной папке проекта есть папка **`firefox-build/`** — она содержит всё необходимое для Firefox с правильным `manifest.json`.

### Шаг 3: Загрузи в Firefox
1. Открой Firefox
2. Введи: `about:debugging#/runtime/this-firefox`
3. Нажми: **"Загрузить временное дополнение..."**
4. Перейди в папку **`firefox-build/`** (не в корень проекта!)
5. Выбери файл **`manifest.json`** внутри `firefox-build/`
6. Нажми: "Открыть"

---

## 🎯 **ВАРИАНТ 2: Использовать готовый ZIP-архив**

### Шаг 1: Скачай ZIP
Скачай файл `notebooklm-importer-firefox.zip` из репозитория.

### Шаг 2: Распакуй
Распакуй архив в любую папку (например, на Рабочий стол).

### Шаг 3: Загрузи в Firefox
1. Открой Firefox
2. Введи: `about:debugging#/runtime/this-firefox`
3. Нажми: **"Загрузить временное дополнение..."**
4. Перейди в распакованную папку
5. Выбери файл **`manifest.json`**
6. Нажми: "Открыть"

---

## 🎯 **ВАРИАНТ 3: Выбрать конкретный файл манифеста**

Если хочешь использовать корневую папку проекта:

1. Открой Firefox
2. Введи: `about:debugging#/runtime/this-firefox`
3. Нажми: **"Загрузить временное дополнение..."**
4. Перейди в **КОРЕНЬ** папки проекта
5. ⚠️ **ВАЖНО!** Выбери КОНКРЕТНЫЙ файл:
   - **`manifest.firefox-persistent.json`** (рекомендуется)
   - ИЛИ `manifest.v2.json`
   - ИЛИ `manifest-firefox.json`
6. **НЕ выбирай `manifest.json`!** (это для Chrome!)
7. Нажми: "Открыть"

---

## ❌ **ПОЧЕМУ БЫЛА ОШИБКА:**

Firefox искал файл с именем `manifest.json` и находил Chrome-версию:
```json
{
  "manifest_version": 3,
  "background": {
    "service_worker": "background.js"  ← ЭТО НЕ РАБОТАЕТ В FIREFOX!
  }
}
```

---

## ✅ **ПРАВИЛЬНЫЙ МАНИФЕСТ ДЛЯ FIREFOX:**

```json
{
  "manifest_version": 2,
  "background": {
    "scripts": ["background.js"],     ← ПРАВИЛЬНО ДЛЯ FIREFOX!
    "persistent": true
  }
}
```

---

## 🎯 **ЧТО ТЕПЕРЬ ДЕЛАТЬ:**

1. **Скачай свежую версию** с GitHub
2. **Используй папку `firefox-build/`** (она уже готова!)
3. **ИЛИ** выбирай конкретный файл: `manifest.firefox-persistent.json`

---

## 📁 **СТРУКТУРА ПРОЕКТА:**

```
notebooklm-importer/
├── firefox-build/              ← ДЛЯ FIREFOX! (готовая папка)
│   ├── manifest.json           ← Правильный манифест (MV2)
│   ├── background.js
│   ├── popup/
│   ├── content/
│   ├── lib/
│   └── icons/
│
├── manifest.json               ← ДЛЯ CHROME! (MV3)
├── manifest-firefox.json       ← Альтернатива для Firefox
├── manifest.v2.json            ← Альтернатива для Firefox
└── manifest.firefox-persistent.json ← Альтернатива для Firefox
```

---

## 💡 **ВАЖНО:**

- ✅ Папка **`firefox-build/`** — готова к использованию в Firefox
- ✅ В ней **`manifest.json`** — это MV2 для Firefox (не Chrome!)
- ❌ **Корневой `manifest.json`** — только для Chrome!

---

## 🚀 **Теперь попробуй снова!**

Используй папку `firefox-build/` и всё заработает! 💪
